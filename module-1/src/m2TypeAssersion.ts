{
    let anything : any;
    anything = "My name is asheq";

    (anything as string).charAt(0);

    function kgToGm(var1: number | string): string | number{
        if (typeof var1 === "string") {
            return parseFloat(var1);

        } else {
            
        }
        return 0 ;
    }
}